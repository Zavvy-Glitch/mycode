#!/usr/bin/env python3

# create a small string
lilstring = "Alta3 Research offers classes on Python coding"

# will return a list/array with the given string split into individual objects
newlist = lilstring.split(" ")

# prints or displays contents of newlist on screen
print(newlist)

# create a list of strings
myiplist = ["192", "168", "0", "12"]

# set singleip as the result of joining the list my iplist around the "."
singleip = ".".join(myiplist)

# display singleip
print(singleip)
